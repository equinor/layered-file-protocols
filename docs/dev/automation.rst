Automation
==========
Protocols are obviously quite similar, so the `lfp::` namespace contains a few
useful constructs that remove some boilerplate.

:code:`#include <lfp/protocol.hpp>`

.. doxygenclass:: lfp::unique_lfp
   :members:

.. doxygengroup:: advance
   :project: lfp
   :content-only:
